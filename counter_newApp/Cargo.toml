[package]
name = "counter_newApp"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1", features = ["full"]}
axum = "0.7"
serde_json = "1.0"
serde = {version = "1.0.229", features = ["derive"]}

[dev-dependencies]
anyhow = "1"
